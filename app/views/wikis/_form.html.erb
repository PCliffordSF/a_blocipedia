<% if current_user.admin? || current_user.premium? %>
  <div class="form-group">
    <%= f.label :private, class: 'checkbox' do %>
      <%= f.check_box :private %> Would you like this Wiki to be private?
    <% end %>
  </div>
<% end %>